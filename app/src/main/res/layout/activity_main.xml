<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    tools:context=".MainActivity">


    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="Llamadas perdidas 0 = sin enviar, 1 = enviado"
        android:textSize="18sp"
        android:textStyle="bold" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/imageView"
            android:layout_width="221dp"
            android:layout_height="117dp"
            android:layout_gravity="center"
            android:layout_weight="0"
            android:scaleType="centerInside"
            app:srcCompat="@drawable/lostp" />
    </LinearLayout>

    <ListView
        android:layout_width="match_parent"
        android:id="@+id/lista_perdidas"
        android:layout_height="322dp" />

    <Button
        android:id="@+id/btn_agradables"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Deseados" />

    <Button
        android:id="@+id/btn_no_agradables"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Indeseados" />

</LinearLayout>